library(tidyverse)
library(janitor)
library(nortest) # contains additional nrmality tests
# bias against scientific notation
# use options(scipen = 999) to restrict display to decimals
# I don't like scientific notation of display
options(scipen = 999)
# loading kidney disease practice dataset withread.csv
# then nesting it in clean_names function from janitor package to automate
# formating of column names to R acceptable format
kd <- clean_names(read.csv('data/kidney_disease_dataset.csv'))
# loading kidney disease practice dataset withread.csv
# then nesting it in clean_names function from janitor package to automate
# formating of column names to R acceptable format
kd <- clean_names(read.csv('./data/kidney_disease_dataset.csv'))
getwd()
# loading kidney disease practice dataset withread.csv
# then nesting it in clean_names function from janitor package to automate
# formating of column names to R acceptable format
kd <- clean_names(read.csv('../kidney_disease_dataset.csv'))
# loading kidney disease practice dataset withread.csv
# then nesting it in clean_names function from janitor package to automate
# formating of column names to R acceptable format
kd <- clean_names(read.csv('../data/kidney_disease_dataset.csv'))
# loading kidney disease practice dataset withread.csv
# then nesting it in clean_names function from janitor package to automate
# formating of column names to R acceptable format
kd <- clean_names(read.csv('datasets/kidney_disease_dataset.csv'))
library(tidyverse)
library(janitor)
library(nortest) # contains additional nrmality tests
# bias against scientific notation
# use options(scipen = 999) to restrict display to decimals
# I don't like scientific notation of display
options(scipen = 999)
# loading kidney disease practice dataset withread.csv
# then nesting it in clean_names function from janitor package to automate
# formating of column names to R acceptable format
kd <- clean_names(read.csv('datasets/kidney_disease_dataset.csv'))
View(kd)
# explorative data analysis
View(kd)
glimpse(kd)
unique(kd$target)
library(dlookr)
# test for normality of age
age_normality <- kd$age_of_the_patient %>%
normality()
# test for normality of age
age_normality <- kd$'age_of_the_patient' %>%
normality()
library(visdat)
# checking for sanity/complete
vis_dat(kd)
vis_miss(kd)
qqnorm(kd$blood_pressure_mm_hg)
qqline(kd$blood_pressure_mm_hg, col='red')
# Shapiro-Wilk test
# suitable fr data between 3- 5000 observations
shapiro.test(kd$blood_pressure_mm_hg)
install.packages("MedDataSets")
# loading the meddatasets package
library(MedDataSets)
# checking the datasets available in the package
data(package = "MedDataSets")
# 1. Install reticulate
install.packages("reticulate")
library(reticulate)
# 2. Install the python package through R
py_install("meddatasets")
py_install("pandas") # meddatasets depends on pandas, so we need to install it too
py_install("numpy") # meddatasets also depends on numpy, so we need to install it too
py_install("scikit-learn") # meddatasets also depends on scikit-learn, so we need to install it too
py_install("pip") # meddatasets also depends on pip, so we need to install it too
py_install("wheel") # meddatasets also depends on wheel, so we need to install it too
py_install("setuptools") # meddatasets also depends on setuptools, so we need to install
py_install("scikit-learn") # meddatasets also depends on scikit-learn, so we need to install it too
# 2. Install the python package through R
py_install("meddatasets")
# 3. Import the package
mds <- import("meddatasets")
py_config() # check if python is configured correctly, this should show you the path to your python installation and the version of python you're usin
# 2. Install the python package through R
py_install("meddatasets")
py_config() # check if python is configured correctly, this should show you the path to your python installation and the version of python you're usin
# Force installation into the 'r-reticulate' environment specifically
py_install("meddatasets", envname = "r-reticulate")
# Force R to use this specific environment
use_virtualenv("r-reticulate", required = TRUE)
library(reticulate)
# Force installation into the 'r-reticulate' environment specifically
py_install("meddatasets", envname = "r-reticulate")
# Force R to use this specific environment
use_virtualenv("r-reticulate", required = TRUE)
#-------------------------------------------------------------------------------
# 3. Import the package
mds <- import("meddatasets")
# 4. Access a dataset (this converts the Python dataframe to an R dataframe)
# Use '$' instead of '.' to access Python methods in R
heart_data <- mds$load_heart_disease()
# 4. Access a dataset (this converts the Python dataframe to an R dataframe)
# Use '$' instead of '.' to access Python methods in R
heart_data <- mds$load_dataset("heart_disease")$data
# 4. Access a dataset (this converts the Python dataframe to an R dataframe)
# Use '$' instead of '.' to access Python methods in R
heart_data <- mds$load_dataset("heart_disease")
# 4. Access a dataset (this converts the Python dataframe to an R dataframe)
# Use '$' instead of '.' to access Python methods in R
heart_data <- mds$load_dataset("cardiovascular_risk")
# 5. Use it like a normal R dataframe
summary(heart_data)
library(tidyverse)
library(dplyr)
library(visdat)
# 4. Access a dataset (this converts the Python dataframe to an R dataframe)
# Use '$' instead of '.' to access Python methods in R
cvd <- mds$load_dataset("cardiovascular_risk")
# 4. Access a dataset (this converts the Python dataframe to an R dataframe)
# Use '$' instead of '.' to access Python methods in R
cvd <- mds$load_dataset("cardiovascular_risk")
#-------------------------------------------------------------------------------
# 3. Import the package
mds <- import("meddatasets")
# 4. Access a dataset (this converts the Python dataframe to an R dataframe)
# Use '$' instead of '.' to access Python methods in R
cvd <- mds$load_dataset("cardiovascular_risk")
# 5. Use it like a normal R dataframe
summary(cvd)
vis_dat(cvd)
glimpse(cvd)
library(reticulate)
#-------------------------------------------------------------------------------
# 3. Import the package
mds <- import("meddatasets")
# Force R to use this specific environment
use_virtualenv("r-reticulate", required = TRUE)
# Force installation into the 'r-reticulate' environment specifically
py_install("meddatasets", envname = "r-reticulate")
# Force R to use this specific environment
use_virtualenv("r-reticulate", required = TRUE)
py_config() # check if python is configured correctly, this should show you the
# Force installation into the 'r-reticulate' environment specifically
py_install("meddatasets", envname = "r-reticulate")
# Force R to use this specific environment
use_virtualenv("r-reticulate", required = TRUE)
# 1. Load reticulate and configure the environment
if (!require("reticulate")) install.packages("reticulate")
library(reticulate)
# It is best practice to create/use a specific environment for your project
# This prevents version conflicts with other Python projects
virtualenv_create("r-med-analysis")
use_virtualenv("r-med-analysis", required = TRUE)
source("D:/teaching/QR Journal Club/meddatasets.R")
# 1. Load reticulate and configure the environment
if (!require("reticulate")) install.packages("reticulate")
library(reticulate)
# It is best practice to create/use a specific environment for your project
# This prevents version conflicts with other Python projects
virtualenv_create("python_in_R")
use_virtualenv("python_in_R", required = TRUE)
# 2. Bulk install Python dependencies
# meddatasets relies on the data science stack (pandas, numpy, etc.)
py_packages <- c("meddatasets", "pandas", "numpy", "scikit-learn",
"matplotlib", "seaborn", "xgboost")
py_install(py_packages, envname = "python_in_R", pip = TRUE)
import numpy as np
reticulate::repl_python()
# 3. Import the Python module
mds <- import("meddatasets")
# 4. List available Python datasets
all_py_datasets <- mds$get_available_datasets()
views(mds) # List available functions in the module
# 4. List available Python datasets
names(mds)
list_datasets(mds)
# 4. List available Python datasets
names(mds)
datasets_mds <- mds$list_datasets()
view(datasets_mds)
print(datasets_mds)
library(MedDataSets)
# View list of datasets available in the R-native package
data(package = "MedDataSets")
# or
py_list_attributes(mds)
mds$DATASETS  # Accessing the DATASETS attributeA
?cvd
